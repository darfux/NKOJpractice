给出一个原字符串string其长度不超过60同时给出k种变换规则（k<=20） 生成规则： 
1． 字符串中的某一个字符可以变换成另一个字符；
2． 形成新的字符串； 
3． 每一次变换都必须为变换规则所规定的。 
例如： 
String = “ACM” k=2 k种规则为： 
C=>A M=>E 那么可生成出的串为（包括原串） ACM AAM AAE ACE 共有4中不同的生成串 

求经过任意次的变换（0次或多次），能生成出多少个不同的字符串？
Input

输入数据第一行为一个字符串，其中字符为a-z,A-Z或0-9中的一个。第二行为正整数k(k<=20)。接下来的k行中包含k种变换规则，每一行包含两个字符，中间用空格分开，表示由左边的字符可以变换为右边的字符。
Output

输出只有一个正整数，表示能生成出多少个不同的字符串。
Sample Input

ACM
2
C A
M E
Sample Output

4


//====Comments====\\

注意：
本题可以循环替换，例如 A => C,  C => D